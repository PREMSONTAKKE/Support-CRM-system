# Frontend (Vite/React) — must be prefixed with VITE_ to be exposed to the client.
# In dev, this points to the local FastAPI server.
# In production, set this to the deployed backend URL (e.g. https://support-crm-api.onrender.com)
VITE_API_URL=http://127.0.0.1:8000
